version: "3.8"

services:
  frontend:
    build: .
    container_name: foodify-frontend
    ports:
      - "8501:8501"
    environment:
      - BACKEND_URL=http://backend:8000
    networks:
      - app-network

networks:
  app-network:
    external: true
